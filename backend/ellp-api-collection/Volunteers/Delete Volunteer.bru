meta {
  name: Delete Volunteer
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/volunteers/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
