meta {
  name: Add Workshop to Volunteer
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/volunteers/:id/workshops/:workshop_id
  body: none
  auth: bearer
}

params:path {
  id: 
  workshop_id: 
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
