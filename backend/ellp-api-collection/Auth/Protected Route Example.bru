meta {
  name: Protected Route Example
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/protected
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
